# 2026-03-27

- Harmony 节点“不显示”的根因已确认是样式转换链路漏映射：
  - 原始 `fill` / `stroke` / `strokeWidth` 没有进入标准 `ViewTreeStyle`，前端因此把节点判定为“无可见样式”。
- 已修复的映射点：
  - `fill -> backgroundColor`
  - `stroke -> borderColor`
  - `strokeWidth -> borderWidth`
- 同步修复了 Harmony 采集器和 demo runtime，避免原始 inspector 走不同实现时出现行为不一致。
- 已补回归测试：
  - `GatewayRoutesTests/testViewTreeSnapshotEndpointMapsHarmonyFillAndStrokeToStandardStyle`
  - `GatewayRoutesTests/testViewTreeSnapshotEndpointAppliesHarmonyButtonDefaultsInGateway`
  - `GatewayViewTreeMockFixturesTests/testRawIngestFixturesDecodeToModel`
- 已刷新 `mocks/ui-tree/*.json` 生成物，保证 mock snapshot 与真实转换逻辑一致。
- 验证通过：
  - `swift test --filter GatewayRoutesTests/testViewTreeSnapshotEndpointMapsHarmonyFillAndStrokeToStandardStyle`
  - `swift test --filter GatewayRoutesTests/testViewTreeSnapshotEndpointAppliesHarmonyButtonDefaultsInGateway`
  - `swift test --filter GatewayViewTreeMockFixturesTests/testRawIngestFixturesDecodeToModel`
