# 2026-03-31

- 发布流水线升级：`.github/workflows/release-cli-tag.yml` 在 GitHub Release 后新增 Homebrew 发布步骤（配置缺失时显式跳过）。
- 新增脚本 `scripts/publish-homebrew-formula.sh`：基于本次 tag 对应的 `dist/cli-release/neptune-<tag>.sha256` 自动更新 tap 仓库 `Formula/<formula>.rb` 并推送。
- 新增发布可配置项：`HOMEBREW_TAP_REPO`（Variable）、`HOMEBREW_TAP_TOKEN`（Secret）、可选 `HOMEBREW_FORMULA`。
- 默认 Homebrew tap 固定为 `linhay/homebrew-tap`；workflow 仅要求配置 `HOMEBREW_TAP_TOKEN`，`HOMEBREW_TAP_REPO` 变为可选覆盖项。
